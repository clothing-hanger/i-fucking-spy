function createFindable(findable,x,y,r,sx,systring)
    return{
        Findable = findable,
        X = x,
        Y = y,
        R = r, --am i even gonna use r     probably not lmao
        SX = sx,
        SY = sy,
        RiddleText = string,
    }
end